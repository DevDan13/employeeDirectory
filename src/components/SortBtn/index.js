import React from 'react'

export default function Sort(props) {
    return (
        <button 
        
        type="submit"
        onClick={props.sort}>
            Sort Employees
        </button>
    )
}
